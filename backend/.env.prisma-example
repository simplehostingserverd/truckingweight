# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=

# Supabase Configuration
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Logging
LOG_LEVEL=debug

# Database Connection URLs
# Original postgres admin connection (keep for migrations and schema updates)
DATABASE_URL_ADMIN=

# Dedicated Prisma role connection (use this for application queries)
DATABASE_URL=

# Connection pooling URL (for high-traffic scenarios)
POOL_CONNECTION_URL=postgresql://prisma_app:prisma_strong_password_change_me@aws-0-us-east-2.pooler.supabase.com:6543/postgres

# Prisma Accelerate URL (if using Prisma Accelerate)
# PRISMA_ACCELERATE_URL=your_accelerate_url_here
